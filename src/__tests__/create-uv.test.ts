import { createUv } from '..';

describe('createUv', () => {
  it('should use config in uv calls', () => {
    // const uv = createUv();
    // const h1 = uv({ base: 'text-3xl font-bold text-blue-400 text-xl text-blue-200' });

    // expect(h1()).toHaveClass('text-3xl font-bold text-blue-400 text-xl text-blue-200');
  });
});
